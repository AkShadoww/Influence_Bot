# ===========================================
# INFLUENCE BOT - Environment Configuration
# ===========================================

# --- ReelStats API ---
BOT_TOKEN=your-reelstats-bot-token
REELSTATS_API_URL=https://campaigns.influence.technology

# --- Slack Configuration ---
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret
SLACK_CHANNEL_ID=C0XXXXXXXXX

# --- Per-category Slack channels (optional; defaults shown) ---
# Each notification type routes to its own channel. Accepts either a channel
# name (e.g. "#content-reviews") or a channel ID (e.g. "C0XXXXXXXXX").
# The bot must be a member of each channel or posts fail with `not_in_channel`.
SLACK_CHANNEL_REVIEWS=#content-reviews
SLACK_CHANNEL_UPLOADS=#content-uploads
SLACK_CHANNEL_PAYMENTS=#payment-reminders
SLACK_CHANNEL_MILESTONES=#breakout-content-alerts
SLACK_CHANNEL_DEADLINES=#creator-deadlines

# --- Email Configuration (jennifer@useinfluence.xyz) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=jennifer@useinfluence.xyz
SMTP_PASSWORD=your-app-password
EMAIL_FROM_NAME=Jennifer - INFLUENCE

# --- Application ---
APP_HOST=0.0.0.0
APP_PORT=3000
DATABASE_URL=sqlite:///influence_bot.db
POLL_INTERVAL_MINUTES=5

# --- Testing ---
# If set, the bot only processes the campaign with this exact name.
# Leave empty in production to process all campaigns.
# TEST_CAMPAIGN_NAME=Dummy testing
